# vim: ft=gitconfig: sw=4:
#
# We use this intermediate file for loading aliases-workspace.gitconfig to be able to
# address both loading by including dot-gitconfig from ~/.gitconfig as
# well as when scripts/hook/git gets used.
#

[include]
    path = defaults.gitconfig
    path = aliases-workspace.gitconfig
